# dotnet-restore

```YAML
    - name: donet publish
      uses : snsinahub-org/dotnet@main
      with:
        path: 'C:\Users\user\'
        temp_path: "temp_path"
        build_configuration: "release"
        newTag: "newTag"
```